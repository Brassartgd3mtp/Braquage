using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Technician : PlayerRole
{
    // Start is called before the first frame update
    void Start()
    {
        _technician = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
